{
  "name": "cat-fact-generator",
  "version": "0.1.0",
  "private": true,
  "homepage": "./",
  "dependencies": {
    "cat-facts": "^1.0.2",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "npm run build && mv build/index.html build/200.html && surge build react-random-cat-fact-generator.surge.sh"
  },
  "devDependencies": {
    "jquery": "^3.3.1"
  }
}
